<section class="w-full min-h-screen bg-fenix-gradient flex flex-col justify-center items-center relative overflow-hidden px-4">
    <!-- Elementos decorativos sin animaciones -->
    <div class="absolute top-0 left-0 w-64 h-64 bg-fenix-red opacity-10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 right-0 w-80 h-80 bg-fenix-blue opacity-10 rounded-full blur-3xl"></div>
    
    <!-- Contenido principal -->
    <div class="relative z-10 text-center max-w-4xl">
        <!-- Logo pequeño -->
        <div class="mb-8 flex justify-center">
            <div class="w-20 h-20 bg-gradient-to-br from-fenix-red via-fenix-orange to-fenix-blue rounded-full flex items-center justify-center">
                <span class="text-white text-3xl font-bold">⚡</span>
            </div>
        </div>

        <!-- Título principal -->
        <h1 class="text-5xl md:text-7xl font-bold mb-4 leading-tight">
            <span class="fenix-accent">ACADEMIA</span><br />
            <span class="text-white">FENIX</span><br />
            <span class="fenix-accent-secondary">TAEKWON-DO</span>
        </h1>

        <!-- Subtítulo -->
        <p class="text-xl md:text-2xl text-gray-300 mb-8 max-w-2xl mx-auto">
            Disciplina, Honor y Excelencia en el Arte del Taekwon-Do ITF
        </p>

        <!-- Separador decorativo -->
        <div class="flex items-center justify-center gap-4 mb-12">
            <div class="w-12 h-1 bg-fenix-orange"></div>
            <div class="w-2 h-2 bg-fenix-gold rounded-full"></div>
            <div class="w-12 h-1 bg-fenix-orange"></div>
        </div>

        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
            <button class="btn-primary">
                Comenzar Hoy
            </button>
            <button class="btn-secondary">
                Ver Horarios
            </button>
        </div>

        <!-- Stats o badges -->
        <div class="mt-16 grid grid-cols-3 gap-8 md:gap-16 max-w-2xl">
            <div class="text-center">
                <div class="text-3xl md:text-4xl font-bold fenix-accent">10+</div>
                <p class="text-gray-400 text-sm mt-2">Años de Experiencia</p>
            </div>
            <div class="text-center">
                <div class="text-3xl md:text-4xl font-bold fenix-accent-blue">500+</div>
                <p class="text-gray-400 text-sm mt-2">Estudiantes</p>
            </div>
            <div class="text-center">
                <div class="text-3xl md:text-4xl font-bold fenix-accent-secondary">15+</div>
                <p class="text-gray-400 text-sm mt-2">Cinturones Negros</p>
            </div>
        </div>
    </div>

    <!-- Flecha hacia abajo interactiva -->
    <a href="#welcome-section" class="absolute bottom-8 left-1/2 transform -translate-x-1/2 text-fenix-orange hover:text-white transition-colors duration-300 flex flex-col items-center gap-1 group" aria-label="Ir a la siguiente sección">
        <svg class="w-6 h-6 animate-bounce cursor-pointer group-hover:scale-110 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
    </a>
</section>